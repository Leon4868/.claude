# 发版文档模板

## 基本信息

| 项目 | 内容 |
|------|------|
| 需求 | [需求描述] |
| 日期 | [YYYY-MM-DD] |
| 开发 | [开发者] |

## 涉及服务及分支

| 服务 | 分支 | 说明 |
|------|------|------|
| [服务名] | `[分支名]` | [变更说明] |

## 发版顺序

> **严格按以下顺序发版，否则会导致编译或运行失败**

1. **[服务1]** — [原因]
2. **[服务2]** — [原因]
3. **[服务3]** — [原因]

## SQL 变更

**脚本文件**：`[SQL文件路径]`

```sql
[SQL脚本完整内容]
```

**执行时机**：[发版前/发版后]，[是否影响线上功能]

---

**如无 SQL 变更，使用以下格式**：

**本次发版无 SQL 变更**

## 初始化 / 配置

### MQ Topic
- **topic**: `[topic名]`，**tag**: `[tag名]`
- [配置说明]

### Nacos 配置
- **配置项**: `[配置key]`
- **默认值**: `[value]`
- [配置说明]

### XXL-JOB 任务
- **任务名**: `[JobHandler名]`
- **Cron**: `[cron表达式]`
- [任务说明]

### Redis Key
- **Key**: `[key名]`
- [用途说明]

### 其他配置
- **[配置类型]**: [配置说明]
- 示例：SDK版本、第三方服务配置、环境变量等

### 无需额外配置
- 不需要新增 Nacos 配置项
- 不需要新增 XXL-JOB 任务
- 不需要新增 Redis key

## 代码审核结果

> 本次变更已通过代码审核，以下是审核中发现的重要事项：

### 需要关注的问题
- **[问题类型]**：[问题描述]
  - 位置：`[文件路径:行号]`
  - 建议：[改进建议]

### 最佳实践建议
- [建议1]
- [建议2]

---

**如无重要发现，使用以下格式**：

**本次代码审核未发现重大问题，代码质量良好。**

## 压测建议

### 是否需要压测

**[需要/不需要]** 压测

**判断依据**：
- [依据1：如涉及核心接口性能优化、新增高并发场景等]
- [依据2：如修改数据库查询逻辑、缓存策略等]
- [依据3：如新增定时任务、MQ消费等]

---

**需要压测的场景示例**：
- ✅ 修改了核心业务接口的查询逻辑
- ✅ 新增了高并发场景的接口
- ✅ 修改了缓存策略或数据库索引
- ✅ 新增了批量处理逻辑
- ✅ 修改了 MQ 消费逻辑

**无需压测的场景示例**：
- ❌ 仅新增字段，不影响查询性能
- ❌ 修复小 bug，不涉及性能相关代码
- ❌ 仅修改配置项或常量
- ❌ 仅新增管理后台功能（低并发场景）

### 压测方案（如需要）

**压测目标**：
- QPS 目标：[具体数值]
- 响应时间：P99 < [具体数值]ms
- 错误率：< [具体数值]%

**压测接口**：
- `[接口路径]`：[接口说明]

**压测数据准备**：
- [数据准备说明]

**压测工具**：
- [JMeter/Gatling/其他]

## 验证方案

### 发版后验证
1. **[验证项1]**：[验证步骤]
   - 示例：查询 `SELECT * FROM table WHERE condition`，确认字段值正确
2. **[验证项2]**：[验证步骤]
   - 示例：触发 XXL-JOB 任务，观察日志中是否有 `关键字` 输出
3. **[验证项3]**：[验证步骤]
   - 示例：调用 API `/api/xxx`，返回状态码 200，响应包含预期数据
4. **[验证项4]**：[验证步骤]
   - 示例：确认非相关功能不受影响，原有流程正常运行

### 回滚方案
- [回滚步骤]
- [数据兼容性说明]
- 示例：新增字段默认NULL，回滚代码后不影响现有功能，无需执行反向SQL
